<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- main style of the app -->
    <style name="Theme.PaulsApp" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- style for the splash screen -->
    <style name="Theme.App.StartSplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/almostBlack</item>
        <item name="postSplashScreenTheme">@style/Theme.PaulsApp</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/animated_logo</item>
    </style>
</resources>